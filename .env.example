# Environment configuration for search proxy
# Copy to .env and adjust.

# Comma separated list of allowed origins. Use * ONLY for development.
CORS_ALLOW_ORIGINS=*

# Target endpoint FULL URL (final JSON, not a redirect). Trailing slash preserved if backend expects it.
TARGET_SEARCH_URL=https://tueducaciondigital.site/ads/getads/

# Google translate API key (optional). If absent, original keywords are forwarded.
GOOGLE_TRANSLATE_API_KEY=AIzaSyC9L_Pu-BkU7S7EEa_D1WfWcxhZTFUOrw8

# Port for local server
PORT=3003
